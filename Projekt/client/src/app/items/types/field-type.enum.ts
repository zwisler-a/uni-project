/** Possible types a field can have */
export enum FieldType {
    string = 'string',
    number = 'number',
    boolean = 'boolean',
    reference = 'reference',
    color = 'color',
    file = 'file',
    date = 'date'
}
